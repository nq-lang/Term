# ⚠️ These keys were pasted in plaintext into a chat session — treat them as
# compromised and ROTATE all of them on each provider's dashboard before you
# rely on this for anything real. This file should never be committed to
# git (it's already in .gitignore below).
#
# ⚠️ NOTE: the "polygon.io" key sent is byte-for-byte IDENTICAL to the
# EODHD_API_KEY sent earlier in this conversation (KczAzxrl...). Polygon and
# EODHD are different companies with different key formats — this is either
# a copy-paste duplication on your end, or the EODHD key got relabeled by
# mistake. Double check this in your provider dashboards; I've kept both
# variables below pointing at the same value for now, but POLYGON_API_KEY
# is unlikely to actually authenticate against Polygon's API as-is.

FRED_API_KEY=331f181e751715dbece9d7ba12fe53be
ALPACA_API_KEY=PKVCGB5HG6FDN3PCT4TAM5NRPD
ALPACA_API_SECRET=
EODHD_API_KEY=KczAzxrl897zcN2jXuNIqICxpF6ZKp7i
POLYGON_API_KEY=KczAzxrl897zcN2jXuNIqICxpF6ZKp7i
TRADIER_API_KEY=iZZHUCjeFGfziYmZEPfU702e1Y3T
